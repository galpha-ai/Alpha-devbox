assistant_name: Devbox
data_root: .devbox-local

container:
  runtime: docker
  image: devbox-runner:latest
  timeout: 5400000
  idle_timeout: 300000
  max_concurrent: 2
  max_output_size: 10485760

web:
  enabled: true
  port: 18092

agents:
  - name: quant-agent
    path: agents/quant-agent

channels:
  - id: "web:*"
    agents:
      - name: quant-agent
        requires_trigger: false
