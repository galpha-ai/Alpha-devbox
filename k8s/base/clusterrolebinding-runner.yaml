apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: devbox-runner
subjects:
  - kind: ServiceAccount
    name: devbox-runner
    # namespace is set by kustomize overlay
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: devbox-runner
